{
  "scripts": {
    "dev": "npm run build && npm start",
    "build": "cd frontend && npm run build",
    "start": "node index.js",
    "lint": "eslint",
    "format": "prettier --write ."
  },
  "dependencies": {
    "eslint": "^9.19.0",
    "prettier": "^3.4.2"
  }
}
